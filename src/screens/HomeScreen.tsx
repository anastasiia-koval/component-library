import { View, Text, Image } from "react-native";
import { Search, CardList } from "../components";

const HomeScreen = () => {
  return (
    <View>
      <Search placeholder="Discover UI library easily :)" />
    </View>
  );
};

export default HomeScreen;
